{% extends 'base.html.twig' %}

{% block body %}
    <div class="container d-flex justify-content-center mt-4">
        <div class="error-page">
            <h1 class="d-flex justify-content-center" style="font-size: 100px">403</h1>
            <p>Es tut uns leid, aber Sie haben keine Berechtigung, auf diese Seite zuzugreifen.</p>
            <a class="d-flex justify-content-center btn btn-primary"  href="{{ path('app_login') }}">Home</a>
        </div>
    </div>
{% endblock %}
